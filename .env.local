# Copy this file to .env.local and edit values before running
POSTGRES_USER=vikunja
POSTGRES_PASSWORD=vikunja
POSTGRES_DB=vikunja

# Optional release version passed as build arg; default in compose is 'local'
RELEASE_VERSION=local
